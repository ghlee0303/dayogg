# Grafana 를 열자마자 질의되도록 Loki 데이터소스를 미리 등록한다 (UI 에서 수동 추가 불필요).
# run-local.ps1 이 GF_PATHS_PROVISIONING 으로 이 디렉터리를 가리킨다.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://localhost:3100
    isDefault: true
